class Box
{
    int length;
    Box(int length)
    {
        this.length=length;
    }
    public static void main(String[] args)
    {
        box b=new box(10);
        System.out.println(b.length);
    }
}